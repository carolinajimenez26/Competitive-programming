#include <bits/stdc++.h>
using namespace std;

int main(void){
  float A, B, C;
  cin >> A >> B >> C;
  cout <<  "MEDIA = " << fixed << setprecision(1) << ( (A * 2) + (B * 3) + (C * 5) ) / 10 << endl;
  return 0;
}
